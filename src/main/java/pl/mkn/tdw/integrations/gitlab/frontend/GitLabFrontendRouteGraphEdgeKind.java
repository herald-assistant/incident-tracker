package pl.mkn.tdw.integrations.gitlab.frontend;

public enum GitLabFrontendRouteGraphEdgeKind {
    ROOT_ROUTES,
    IMPORT,
    RE_EXPORT,
    CHILDREN,
    LOAD_CHILDREN,
    COMPONENT,
    LOAD_COMPONENT
}
