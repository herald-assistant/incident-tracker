package pl.mkn.tdw.features.runtimeconfigurationverification.scope;

public record RuntimeConfigurationSystemOption(
        String id,
        String label,
        String configurationDirectory
) {
}
